#include <stdio.h>
main(){
    float height=7.0, width=5.0;
    float perimeter = 2.0*(height+width);
    float area = height*width;
    printf("Perimeter of the rectangle = %.2f \n", perimeter);
    printf("Area of the rectangle = %.2f", area);
}
